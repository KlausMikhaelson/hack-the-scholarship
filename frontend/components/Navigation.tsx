'use client';

// This component is now deprecated - navigation is handled in layout.tsx
// Keeping for backwards compatibility, but it renders nothing
export default function Navigation() {
  return null;
}
