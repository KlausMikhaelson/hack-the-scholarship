# Services

Business logic layer. Services contain reusable business logic that can be used by controllers.

Example:
- `example.service.js` - Example service with database operations
- `auth.service.js` - Authentication service
- `email.service.js` - Email service
- etc.

